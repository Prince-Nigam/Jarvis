/**
 * rightpanel.js — activity log and right panel init
 */
(function () {
    'use strict';

    window.initRightPanel = function () {
        if (window.addLog) {
            window.addLog('System ready', 'success');
        }
    };
}());
